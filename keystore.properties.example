# Copy these values to ~/.gradle/gradle.properties or environment variables.
# Never commit a real keystore or real passwords to the repository.
RELEASE_STORE_FILE=/absolute/path/to/midi-game-engine-upload.jks
RELEASE_STORE_PASSWORD=replace-me
RELEASE_KEY_ALIAS=midi-game-engine-upload
RELEASE_KEY_PASSWORD=replace-me
